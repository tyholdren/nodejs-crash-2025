import { getPosts } from './postController.js';

console.log(getPosts());

/* const { generateRandomNumber, celsToFahr } = require('./utils');

console.log(`random number: ${generateRandomNumber()}`);
console.log(`cels to fahr: ${celsToFahr(10)}`);
 */
